<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Menu</title>
</head>
<body style="background: rgb(101, 92, 99)">
    <%- include('./partials/nav') %>
    <h1><Full Menu></h1>
    <ul>
        <% restaurant.menu.forEach((item, index) => { %>
        <li><a href="<%=item.id%>">
            <%= item.name %></a>   <%= item.price %>€ <br>"<%=item.details%>" </li>
        <% }) %>
    </ul>
</body>
</html>
